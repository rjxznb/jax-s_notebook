[rjxznb/jax-s_notebook at master](https://github.com/rjxznb/jax-s_notebook/tree/master)

## 1. 简单工厂模式

工厂：实例化对象的类，假设当前有一个基类，他有多个子类实现虚函数的不同功能，那么后续我们就可以通过工厂类来创建实例对象，即使后面添加新的功能只需要构建一个子类就可以，之后在工厂类的函数里面添加一个switch case分支就可以啦，避免了修改其他代码的情况；

我们通过计算器类来介绍该模式；

## 2. 策略模式

此时要实现一个收银系统，计算每次购物的总额，`totle = commodity1*price1+commodity*price2+...+commodityN*priceN`，此时有一个问题，就是商场可能会有活动，打m折，满m元减n元等活动，这时我们如果还按照工厂模式的话，就需要设置一个父类，之后提前设置好所有的打折类（打1/2/3...8/9折），以及其他活动类来继承这个父类，最后通过一个工厂类来通过switch来选择具体的活动类构造实例对象进行计算；但是这种方法需要为每一种活动情况单独设置一个子类，打1折和打9折实际都是同一种模式，而满m元减n元实际上也是一种模式，我们只需要实现这两种模式的子类即可，之后在每个子类内部实现的虚函数中再具体划分情况去讨论，这就是优化后的工厂模式；下面介绍另外一种更合适的设计模式；

<img src="C:\Users\rjx\系统\Desktop\DesignPattern\image\屏幕截图 2025-05-28 063352.jpg" style="zoom:50%;" />

策略模式：他定义了一个算法家族，分别封装起来，让他们之间**可以相互替换，**从而使得算法的替换并不影响用户的使用；**他的核心点就是通过一个上下文对象，在该对象内部维护一个父类指针成员，他不同于工厂只需要一个工厂对象就能一直创建算法实例，策略模式是通过对一个上下文对象赋值不同的算法，之后通过该上下文对象进行算法的调用；**

<img src="C:\Users\rjx\系统\Desktop\DesignPattern\image\屏幕截图 2025-05-28 070740.jpg" style="zoom:50%;" />













